import { css } from '@styled/css';

export const body = css({
  flexGrow: 1,
});

export const footer = css({
  marginTop: '8',
});

export const effectiveAt = css({
  fontFamily: 'mono',
  fontSize: 'sm',
  color: 'fg.muted',
});
