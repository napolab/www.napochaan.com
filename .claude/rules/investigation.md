# Investigation Guidelines

1. Use `AskUserQuestion` when specifications are unclear
2. Use `WebSearchTool` for research instead of relying on memory or assumptions
3. Output investigation findings to `@reports` directory
