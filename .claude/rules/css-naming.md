---
description: Ban generic names (container, wrapper, box) in CSS — use xxxRoot pattern
paths:
  - "**/*.css.ts"
---

Do not use `container`, `wrapper`, `outer`, `inner`, or `box` in style names.
Use `xxxRoot` instead (e.g., `cardRoot`, `sectionRoot`, `modalRoot`).
